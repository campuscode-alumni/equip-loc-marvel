<div class="container theme-showcase" role="main">

  <div class="jumbotron">
    <%= image_tag("marvelcabecalho.jpg", width: '100%') %>
  </div>

  <div>
    <h3><%= flash[:alert] %></h3>
  </div>


<%= link_to 'Solicitar Orçamento', new_budget_path, class: "btn btn-lg btn-primary" %>

<div id= 'all-contracts'>
  <% @contracts.each do |contract| %>
    <%= contract.id %>
    <%= contract.customer.name %>
    <%= number_to_currency contract.total_amount %>
    <%= l(contract.start_date) %>
    <%= l(contract.end_date) %>
  <% end %>
  <%= flash[:empty] %>
</div>

<div id='contracts-ending-today'>
    <% @contracts_end_today.each do |contract_end| %>
      <%= contract_end.id %>
      <%= contract_end.customer.name %>
      <%= contract_end.delivery_address %>
      <%= contract_end.equipment.count %> equipamentos
    <% end %>
    <%= flash[:empty_today] %>
</div>
</div>
